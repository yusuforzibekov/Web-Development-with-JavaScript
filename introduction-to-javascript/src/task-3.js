"use strict";

const firstNum = parseInt(prompt("Enter the first integer number:"), 10);

const secondNum = parseInt(prompt("Enter the second integer number:"), 10);

const product = firstNum * secondNum;

alert(product);